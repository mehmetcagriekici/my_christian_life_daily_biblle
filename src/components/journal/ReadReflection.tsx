"use client";

//imports
import { TReflection } from "@/utils/types";

export default function ReadReflection({
  reflection,
}: {
  reflection: TReflection;
}) {
  //toggle between ReflectionForm and ReadReflection
  //only display if there is a selected reflection, and toggle is on the ReadReflection
  return <div></div>;
}
